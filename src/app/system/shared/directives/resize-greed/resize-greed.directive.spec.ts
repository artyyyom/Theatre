import { ResizeGreedDirective } from './resize-greed.directive';

describe('ResizeGreedDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeGreedDirective();
    expect(directive).toBeTruthy();
  });
});
