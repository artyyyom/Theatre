import { FilterPerformancesStagePipe } from './filter-performances-stage.pipe';

describe('FilterPerformancesStagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPerformancesStagePipe();
    expect(pipe).toBeTruthy();
  });
});
