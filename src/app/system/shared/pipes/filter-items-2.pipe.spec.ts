import { FilterItems2Pipe } from './filter-items-2.pipe';

describe('FilterItems2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItems2Pipe();
    expect(pipe).toBeTruthy();
  });
});
